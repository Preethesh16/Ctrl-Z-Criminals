# Copy to .env and adjust. Never commit .env (git-ignored).
POSTGRES_PASSWORD=change-me-strong
SECRET_KEY=change-me-strong

# Optional LLM assist (column-mapping suggestions + report narrative).
# Keep DISABLED when working with real/confidential statements.
# LLM_ENABLED=false
# ANTHROPIC_API_KEY=
